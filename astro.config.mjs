// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    output: 'static', // Esto es obligatorio para GitHub Pages
  base: '/Porfolio/',
});
